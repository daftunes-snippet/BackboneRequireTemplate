define([
	'backbone',
], function (Backbone) {

	'use strict';

	var DayModel = Backbone.Model.extend({
		defaults: {
			
		},
		parse: function (data) {
			var map = {
				
			};
			return map;
		}
	});

	return DayModel;
});